<%
    controller = params[:controller]
    category = controller.capitalize
    id = params[:id]
    action = params[:action]
    if controller == "home"
        category = ""
        item = ""
        identifier = ""
    elsif controller == "softwares"
        category = "Software Programs"
        if !id.nil?
            item = Software.find_by_slug(id)
            identifier = item.title
        end
    elsif controller == "skills"
        if !id.nil?
            item = Skill.find_by_slug(id)
            identifier = item.title
        end
    elsif controller == "years"
        if !id.nil?
            item = Year.find_by_value(id)
            identifier = id
        end
    elsif controller == "jobs"
        if !id.nil?
            item = Job.find(id)
            identifier = item.employer
        end
    elsif controller == "job_skills"
        category = "Job/Skill Links"
    elsif controller == "job_softwares"
        category = "Job/Software Links"
    elsif controller == "job_years"
        category = "Job/Year Links"
    end
%>
<ul class="breadcrumbs">
    <li>
        <% if controller != "home" %>
            <a href="/" rel="internal" title="">Home</a>
        <% else %>
            <a>Home</a>
        <% end %>
    </li>
    <% if category != "" %>
        <li>
            <span class="breadcrumb-separator">&gt;</span>
            <% if action  == "index" %>
                <a><%= category %></a>
            <% else %>
                <a href="/<%= controller %>/" rel="internal" title="See all <%= category %>"><%= category %></a>
            <% end %>
        </li>
    <% end %>
    <% if action != "" && action != "index" %>
        <li>
            <span class="breadcrumb-separator">&gt;</span>
            <% if action == "show" and !id.nil? %>
                <a><%= identifier %></a>
            <% else %>
                <% if !id.nil? %>
                    <a href="/<%= controller %>/<%= params[:id] %>" rel="internal" title="">
                        <%= identifier %>
                    </a>
                <% else %>
                    <a><%= action %></a>
                <% end %>
            <% end %>
        </li>
    <% end %>
</ul>
<% # TODO: this doesn't handle nested resources! %>
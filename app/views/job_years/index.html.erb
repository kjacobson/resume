<h1>Listing job_years</h1>

<table>
  <tr>
    <th>Job</th>
    <th>Year</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @job_years.each do |job_year| %>
  <tr>
    <td><%= job_year.job_id %></td>
    <td><%= job_year.year_id %></td>
    <td><%= link_to 'Show', job_year %></td>
    <td><%= link_to 'Edit', edit_job_year_path(job_year) %></td>
    <td><%= link_to 'Destroy', job_year, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job year', new_job_year_path %>

<% content_for :head do %>
    <hgroup>
        <h1>
          <%= decode(@software.title) %>
        </h1>
    </hgroup>
<% end %>

<% content_for :main do %>
    <section class="primary">
        <h2>Positions involving <%= decode(@software.title) %></h2>
        <%= render :partial => 'jobs/small_list' %>
    </section>
<% end %>